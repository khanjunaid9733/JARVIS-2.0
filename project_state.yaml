version: "2.0.0"
project:
  name: "JARVIS"
  full_name: "Just A Rather Very Intelligent System"
  workspace: "F:/JARVIS2.0"
  type: "persistent_cognitive_operating_system"
  updated_at: "2026-09-18T21:10:00Z"

spec:
  version: "1.0.0-freeze"
  frozen_at_utc: "2026-09-14T15:38Z"
  canonical_path: "docs/MASTER_BUILD_SPEC.md"
  sha256: "5df073f751533f3ca447720b28e83d8d9eaadb78770981da539eb18d45ac5313"
  spec_version_history:
    - version: "1.0.0-freeze"
      frozen_at_utc: "2026-09-14T15:38Z"
      sha256: "5df073f751533f3ca447720b28e83d8d9eaadb78770981da539eb18d45ac5313"
      change: "M1 readiness patch applied: ADR-005..009 accepted; spec s134 (M1 cut list, milestone ladder mapping, NAT-01..05) merged; Python pinning window (>=3.12,<3.14) established"

status:
  implementation: "in_progress"
  milestone: "m0_completed_m1_readiness_patched"
  m0_audit: "completed_verdict_m1_buildable_after_setup"
  m1_readiness_patch: "applied_freeze_commit_pending"
  m1_kernel: "must_tier_complete_pending_gate_review"
  event_log: "implemented_merged_d5d5567"
  creator_identity: "implemented_merged_9b3955b"
  intent_abi: "implemented_merged_81ceb23"  # module 3 (intent ABI + static validation), merged into main
  capability_registry: "removed_at_12ba475"  # superseded module-4 interpretation deleted per gate review; authoritative module is registry.py (03b8f3c)
  registry: "implemented_merged_03b8f3c"  # authoritative module-4 spec: in-memory CapabilityRegistry + ProviderAdapter Protocol (interface), NAT-02 deterministic half
  registry_events: "implemented_merged_5127753"  # Freebuff F5: optional capability.provider_added / provider_revoked writer (log=None preserves in-memory-only)
  memory_projection: "implemented_merged_16afde0"  # module 7: deterministic projection state + NAT-03 state digest (s134.1)
  effect_envelope: "implemented_merged_8f1ba9a"  # module 8: single-effect prepare/authorize/commit/verify envelope (s83/s98); zero-effects half of NAT-01; idempotency concurrency fix merged cca3d29 (module 8b). Post-review additive seam (module 14, b41509b): optional mission_id stamped on emitted effect.* events; default None = prior behavior byte-identical
  policy: "implemented_merged_ce0ffa1"  # module 9: deterministic policy-viability gate (s80.5) - capability/budget/risk-autonomy/privacy-trust; standalone seam (not yet wired)
  done_gate: "implemented_merged_0e4a92a"  # module 10: deterministic completion gate (s81.3) + gated memory.write path (s84.4/s127.1); full lifecycle FSM deferred as STRETCH per s134.1
  bootstrap_cli: "implemented_merged_95d4d10"  # module 11: idempotent CoreService bootstrap + jarvis init/say/explain/replay --verify (s127.1); deterministic offline extractive recall; hatchling console script
  observability: "implemented_merged_a87e5ca"  # module 12: jarvis.* OpenTelemetry spans (s110.1); additive optional observer on EventLog/PolicyEngine/ModelGateway; opentelemetry-sdk dependency
  m1_acceptance: "implemented_merged_e9e2e10"  # module 13: NAT-01..05 end-to-end + s112 invariant suite (tests/acceptance/test_m1_acceptance.py)
  mission_lifecycle: "implemented_merged_b41509b"  # module 14 (STRETCH M1.1, s134.1): deterministic mission-lifecycle FSM (pending -> executing -> completed|refused|compensated) as a pure fold over the mission stream slice; DATA transition table; lifecycle.* audit events on stream 'mission'; idempotent advance(); NAT-03-style state digest; NAT-05 (consumes task.completed, never appends it); build_effect_engine threads observer + mission_id (F-E15 + F-M14-1). Antigravity adversarial pass reconciled @ f31f87f (F-M14-1..6: 5 fixed, 1 documented). Suite 267 passed
  m6_gate_carryover: "implemented_merged_814d8bd"  # pre-gate carry-over from the Module-6 red-team queue: F8 (pyproject [dependency-groups] dev pytest + conftest interpreter gate rc=3), F5 (strict _constraint_matches: exact-on-0.x, prerelease/padding rejected, _NUMERIC_VERSION), F6 (schema_sha256 bound to schema_id on adapter args + ValidatedOutput), F14 (runtime failover to fallback_provider_id on ProviderTransportError), F3/F13 (resolve_version_for_provider -> contract_version tracks the ACTIVE provider). F2/F10 already applied pre-03622e7. 231 passed (+10 regression tests)
  m1_gate_review: "findings_reconciled_merged_1a1311d"  # Freebuff M1 report (8 findings, no CRITICAL) reconciled + merged to main; suite 243 passed
  m1_gate_review_findings:
    F-E15_critical_gate: "fixed_merged_1a1311d"  # HIGH / gate-relevant (§110.1): EffectEnvelopeEngine now accepts observer; run wrapped in jarvis.effect.execute span, postcondition check under jarvis.verify.postconditions; additive seam, NoOp default inert
    F-A1: "fixed_merged_1a1311d"  # MEDIUM: _committed_keys rebuilt from effect.verified events at engine construction -> idempotency survives restart; evidence test flipped (restart returns prior envelope, no re-execution)
    F-A2: "fixed_merged_1a1311d"  # LOW: effect.refused stamped with cause_event_id/correlation_id -> prepared event
    F-B6: "fixed_merged_1a1311d"  # MEDIUM: _privacy_violation now fail-closed on empty-contract manifests under non-public privacy class
    F-D13: "fixed_merged_1a1311d"  # MEDIUM: ANSWER_MIN_CONFIDENCE=0.5 floor; low token-overlap queries now report "no relevant memory" instead of a confident guess
    F-C9: "creator_ratified_permissive_default_2026-09-18"  # MEDIUM: RULING = ratify as-is — non-creator MemoryWriter allowed, provenance carried, authority deferred to the M2 key-based module (same half as NAT-02); pinned by test_non_creator_writer_commits_with_provenance
    F-F17: "fixed_merged_1a1311d"  # LOW: noop-observer test strengthened (round-trip + no spans); NAT-01 pins EffectFailure.phase
    F-G20: "documented_merged_1a1311d"  # LOW: NAT-03 docstring now states same-log re-replay guarantee vs program re-run
  m1_signoff: "creator_signed_off_2026-09-18"  # F-C9 ratified as-is; all M1 gate items closed (see m1_gate_review_findings); MUST-tier complete + dual-verified (Antigravity df72c4a, Freebuff red-team reconciled 1a1311d)
  m1_1_signoff: "creator_signed_off_2026-09-18 @ 160a3c2"  # M1.1 STRETCH (modules 14-17) FROZEN + signed: 292 passed; module-14 pass (F-M14-1..6) + Antigravity audit (F-M15-1..F-M17-2) all reconciled; live Groq smoke verified; offline section 127.1 byte-identical; see docs/M1_1_SIGNOFF.md (residuals: manifest-DAG validation -> M2, NAT-02 key-authority -> M2, 0-token accounting until adapter usage, JARVIS_MODEL_NAME selection, env/visibility)
  m1_e15_residual: "consumed_by_module_14_b41509b"  # F-E15 seam was fixed + tested at M1 gate; module 14 now actually constructs EffectEnvelopeEngine(s) via MissionLifecycleOwner.build_effect_engine and threads service.observer + mission_id — the "no consumer" situation is closed (jarvis CLI still constructs none today; that is orchestration-dependent)
  m1_gate_review_prompt: "docs/M1_REDTEAM_PROMPT_FB.md"  # paste-ready dispatch prompt for Freebuff (frozen target df72c4a); worktree kept pristine so reviewer preconditions hold
  m1_gate_review_predecessor: "superseded"  # earlier request docs/M1_REDTEAM_REQUEST.md targeted main @ 814d8bd in worktree F:\JARVIS-rt-m1 (branch review/m1-gate @ a6a16aa); superseded by the df72c4a freeze (M6 carry-over + s112 fix now in scope)
  m1_must_tier_verification: "antigravity_verified_cross_environment_confirmed_f8d8a77"  # 2026-09-18: independent read-only audit of all 12 s134.1 MUST items + NAT-01..05 + s112 subset on main @ f8d8a77; 231 passed; verdict CROSS_ENVIRONMENT_CONFIRMED: M1_MUST_TIER_COMPLETE. Gate remains open pending Freebuff red-team + creator sign-off
  m2_memory_os: "kickoff_drafted_pending_ratification"  # M1.1 signed off 2026-09-18 @ 160a3c2; docs/M2_KICKOFF.md proposes 10 packages (memory API/episodic trace, embedding+retrieval seams, consolidation, verification, PII seam, FS effect+sandbox, intent-to-manifest compilation+DAG validation, key-based authority, budgets/rate limits+loop breaker, semantic checkpoints+recovery); M2 proof: read file -> evidence -> memory trace -> restart -> recover
  m3_missions: "not_started"
  m4_multimodal: "not_started"
  m5_robotics: "not_started"

authority:
  architecture: "docs/MASTER_BUILD_SPEC.md"
  decisions: "docs/DECISIONS.md"
  glossary: "docs/GLOSSARY.md"
  m0_audit: "docs/M0_AUDIT.md"
  development_context: "DEVELOPMENT_CONTEXT.md"
  instructions: "AGENTS.md"

gates:
  m0:
    verdict: "M1_BUILDABLE_AFTER_SETUP"
    audited_at_utc: "2026-09-14T15:38Z"
    audit_revision: 2
    open_setup_items:
      - "configure model backend (install Ollama or select remote OpenAI-compatible endpoint)"
  m1:
    entry_conditions:
      - "model backend configured; M0 rows re-verified -> final verdict M1_BUILDABLE_NOW"
      - "M1 cut list (spec s134.1) implemented: MUST tier complete; STRETCH (M1.1) optional"
      - "smoke test (spec s127.1) plus NAT-01..05 (spec s134.3) passing"

nats:
  nat_01:
    bind: "capability registry + intent ABI (constitution 6/11)"
    status: "implemented"
    note: "both halves now demonstrated end-to-end in tests/acceptance/test_m1_acceptance.py: ungranted capability -> ValidationFailure, no Manifest; effect outside manifest -> EffectFailure(refused) with adapter.calls == 0"
  nat_02:
    bind: "creator identity + capability registry authority (constitution 5)"
    status: "partial"
    note: "deterministic half implemented (test_nat_02_non_creator_registration_is_rejected: non-creator principal_id -> AuthorityUnavailable, registry unchanged); signature-verification half of authority deferred until a key-based authority module lands (documented M1 trust model: principal_id equality)"
  nat_03:
    bind: "event log -> minimal memory projection (constitution 20)"
    status: "implemented"
    note: "digest = sha256(canonical JSON of projection state: last_seq, event_count, streams, providers, memories) in kernel/memory_projection.py (MemoryProjection.digest()); replaces the interim EventLog.projection_digest() stream proxy per s134.1. Event bytes/timestamps excluded; identical logs -> identical digest"
  nat_04:
    bind: "event log (constitution 10)"
    status: "implemented"
    note: "replay() always verifies; tamper halts with EventIntegrityError"
  nat_05:
    bind: "deterministic FSM / done-gate (constitution 12)"
    status: "implemented"
    note: "deterministic done-gate implemented in kernel/done_gate.py (module 10): CompletionGate.declare_completion is the only completion path and emits task.completed ONLY on a passing gate decision; failed evidence emits task.completion_refused, never a completion event (tests test_nat_05_completion_without_evidence_is_refused / _partial_evidence_still_refused). Full mission-lifecycle FSM is STRETCH (M1.1, s134.1) and not required for this NAT; the agent-runtime half is simulated by direct calls (no agent runtime in M1)"

m1_definition_of_done:
  s134_1_must: "complete"  # event store, creator identity, intent ABI, registry, adapter iface, model gateway, generate_structured, memory projection, CLI init/say/explain, replay --verify, OTel jarvis.* spans, 5 NATs
  s112_invariants: "m1_subset_green; deferred: budget accounting (M2), quarantine/cancellation/circuit-breakers (M3), secret-scanning (M2)"
  s127_1_smoke: "observable_offline; model-backed say + full explain rendering DONE (STRETCH M1.1, modules 15/17) — gateway path engages only when JARVIS_MODEL_API_KEY/JARVIS_MODEL_BASE_URL are set; offline byte-identical"
  disclosed_deviations:
    - "model backend is remote OpenAI-compatible (Groq) per ADR-010, not a local Ollama adapter as s134.1 literally names"
    - "jarvis say question path is model-backed when JARVIS_MODEL_API_KEY/BASE_URL/NAME are configured; deterministic extractive otherwise (module-6 adapter default llama-3.3-70b-versatile is decommissioned on Groq — M1.1 picks openai/gpt-oss-120b via JARVIS_MODEL_NAME)"
    - "budget tokens recorded as 0 until an adapter surfaces usage (module-6 interface unchanged per M1.1 invariant; tokens payload key is the forward seam)

current_task:
  status: "m1_1_frozen_signed_off"
  phase: "m2_kickoff_drafted"
  objective: "m2_memory_os_initialized"
  next_step: "ratify M2 work packages (docs/M2_KICKOFF.md), then implement M2.1 memory API + episodic trace"

m1_1_stretch:
  status: "modules_15_17_reconciled"  # module 14 + module 15 (model-backed answer) + module 16 (budget ledger) + module 17 (full explain) DONE merged; Antigravity audit (46e0d98) findings F-M15-1/F-M16-1/F-M17-1/F-M17-2 all fixed + regression tests
  entry_conditions:
    - "M1 sign-off recorded (DONE 2026-09-18)"
    - "model backend reachable: ADR-010 remote OpenAI-compatible (Groq) endpoint configured with a key"
    - "main pushed to origin / main push protection resolved (DONE 2026-09-18 @ 2a7f33c)"
    - "deterministic kernel invariant to M1.1 work (additive modules only; no src/ behavior changes to modules 1-13)"
  scope_items: "spec s134.1 STRETCH (M1.1): [DONE] full mission-lifecycle FSM (module 14, b41509b); [DONE] full explain cause-chain rendering (module 17, f89ad5f); [DONE] model-backed say/explain rendering (module 15, f89ad5f+b45add7); [DONE] budget accounting display (module 16, f89ad5f); non-goal: M2 memory OS, M3 missions, M4 multimodal, M5 robotics"
  kickoff: "docs/MODULE14_KICKOFF.md"
  module14:
    merged: "b41509b"
    reconciled_against: "antigravity_adversarial_pass_2026-09-18"  # F-M14-1..6; 5 fixed @ f31f87f, F-M14-6 accepted+documented (single-writer advance() contract); see docs/MODULE14_IMPLEMENTATION.md §7
    rejection: "none"
  module15:
    implemented: "f89ad5f"
    note: "model-backed structured answering; additive input_text seam on ModelGateway/OpenAICompatibleAdapter (question+recalled memories grounded, verified live); question.asked appended only on gateway path (offline byte-identical); JARVIS_MODEL_NAME default openai/gpt-oss-120b (module-6 default decommissioned on Groq); model_name provenance; see docs/MODULE15_IMPLEMENTATION.md"
  module16:
    implemented: "f89ad5f"
    note: "BudgetLedger pure fold: question.asked + mission.started allocation; per-mission key (mission_id or stream_id); honest 0-token accounting until an adapter reports usage; see docs/MODULE16_IMPLEMENTATION.md"
  module17:
    implemented: "f89ad5f"
    cli: "b45add7"
    note: "explain_event pure read-only renderer (cause chain, lifecycle state/transitions, model provenance + recalled memories, policy/effect counts, budget slab, memory content); CLI full rendering keeps section 127.1 assertions stable; see docs/MODULE17_IMPLEMENTATION.md"
  adversarial_review: "completed_report_docs_M1_1_AUDIT_46e0d98"  # Antigravity review of modules 15-17 completed (verdict M1.1_RECONCILIATION_READY, 4 findings)
  reconciled_findings:
    F-M15-1: "fixed"  # MEDIUM: additive mission_id on answer_question/_record_question; question.asked stamped -> budget folds to mission slab (not session); test_mission_scoped_question_stamps_mission_id_for_budget
    F-M16-1: "fixed"  # MEDIUM: non-numeric/negative attempts guarded via _bounded_count (no replay poison pill); test_non_numeric_attempts_do_not_break_the_fold
    F-M17-1: "fixed"  # MEDIUM: cause-chain walk cycle-safe (seen set terminates on repeated node); test_self_referential_and_cyclic_cause_chains_terminate
    F-M17-2: "fixed"  # LOW: Field(default_factory=list) for lifecycle_transitions/recalled_memories; test_explanation_defaults_are_fresh_lists

constraints:
  architecture_freeze: true
  no_automatic_installation: true
  no_runtime_implementation_before_m0: false  # M0 is completed; M1 entry gated only on model backend
  enforce_substitution_seam: true
  require_creator_authority: true
  deterministic_kernel_authoritative: true

truth_protocol:
  levels:
    - VISION
    - PROPOSAL
    - ACCEPTED_DESIGN
    - IMPLEMENTED
    - VERIFIED

agents:
  opencode:
    platform: "OpenCode CLI"
    persona: "Big Pickle"
    role: "primary_implementation_engineer"
    responsibilities:
      - "Inspect repository and context"
      - "Implement accepted designs in src/"
      - "Write automated tests in tests/"
      - "Preserve architecture invariants"
    can_redefine_architecture: false

  freebuff:
    platform: "Freebuff / DeepSeek"
    persona: "DeepSeek Reasoner"
    role: "adversarial_architecture_and_research_reviewer"
    responsibilities:
      - "Stress-test proposed designs"
      - "Identify failure modes and concurrency bottlenecks"
      - "Propose invariant matrices and test scenarios"
      - "Perform trade-off analysis on libraries/models"
    can_redefine_architecture: false

  antigravity:
    platform: "Antigravity IDE"
    persona: "Gemini"
    role: "independent_engineering_and_quality_reviewer"
    responsibilities:
      - "Audit real file/system state against claims"
      - "Verify security boundaries and provider substitution seams"
      - "Validate test coverage and typing correctness"
      - "Ensure documentation and context synchronization"
    can_redefine_architecture: false
